const express = require('express')
const router = express.Router();
const engineerController = require('../../controller/user/user')

//router.post('/user', userController.REGISTER)

module.exports = router